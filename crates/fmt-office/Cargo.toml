[package]
name = "viewit-fmt-office"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "ViewIt OOXML (docx/xlsx/pptx) + legacy binary parsers — Phase 3"

[dependencies]
viewit-core-types = { path = "../core-types" }
anyhow.workspace = true
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
bytes.workspace = true

# Phase 3.2 XLSX + 3.4 ODS
calamine.workspace = true
# Phase 3.1 DOCX (lightweight built-in reader)
docx-rust.workspace = true
# Phase 3.3 PPTX (zip reader)
zip.workspace = true
# Phase 3.7 Encrypted Office
office-crypto = { workspace = true, optional = true }

# Phase 3.3 PPTX + 3.4 ODT/ODP — quick-xml for OOXML zip+XML parsing
quick-xml.workspace = true
cfb.workspace = true

[features]
default = []
support-encrypted = ["dep:office-crypto"]
